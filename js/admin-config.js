// Mot de passe "léger" pour la page admin — change-le !
// ATTENTION : ce n'est PAS une vraie sécurité (le site est 100% statique,
// n'importe qui d'un peu curieux techniquement pourrait lire les données
// via la clé publique Supabase). C'est juste un frein pour que les
// participant·es ne tombent pas dessus par hasard. Ne mets rien de
// sensible (pas de mot de passe, coordonnées bancaires, etc.) dans les
// données collectées.
const ADMIN_PASSWORD = "poupi2026";
